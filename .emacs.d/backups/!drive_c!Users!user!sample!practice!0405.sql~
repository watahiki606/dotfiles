declare
cursor emp_cur(d_no number) is select empno, ename from emp where deptno = d_no;
d_var number;
begin
d_var := &deptno;
for emp_rec in emp_cur(d_var) loop
dbms_output.put_line(emp_rec.empno||' '||emp_rec.ename);
end loop;
end;
/
