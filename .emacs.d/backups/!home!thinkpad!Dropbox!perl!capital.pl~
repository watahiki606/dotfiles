#iオプションを使って大文字と小文字を区別しないでパターンマッチを行う
#小文字のみで構成
$string =  "perl";
print "「perl」にマッチ：　$string\n" if ($string =~ /perl/i);

#大文字のみで構成
$string = "PERL";
print "「perl」にマッチ:　$string\n " if ($string =~ /perl/i);

#大文字と小文字混合で構成
$string = "Perl";
print "「perl」にマッチ：　$string\n" if ($string =~ /perl/i);

#実行結果はマッチング対象の大文字小文字を変えているがiオプションによってすべて同じ文字列とみなされるため
#３つともパターンperlにマッチする。
