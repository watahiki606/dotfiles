#入寮した文字列と数値をパックして表示した後、元の文字列に戻して再表示する。

#packとunpackのテスト
print "文字列を入力してください＞";
$string = <STDIN>;
chomp($string);
$length = length($string);

print "数値を入力してください＞";
$number = <STDIN>;
chomp($number);

#パックする
$packed = pack("A${length}i", $string, $number);
print "パック文字列： $packed\n";

#アンパックする
($string2, $number2) = unpack("A${length}i", $packed);
print "元の文字列： $string2 元の数値： $number2\n";
