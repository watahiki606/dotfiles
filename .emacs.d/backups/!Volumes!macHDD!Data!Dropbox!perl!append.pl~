#output.txtを追加書き出し用としてオープンし文字列を追加する

#追加出力ファイルに文字列を書き出す
# append.pl

#追加出力ファイルとしてオープン
open(F, ">>output.txt") || die "output.txtに追加できません。$!\n";

#文字列をファイルハンドルFに書き出し
print F "こんばんは、花子さん\n";

close(F);
